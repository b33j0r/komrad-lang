Server = {
	port = 8080
	host = "localhost"
	listener = null

	[start] {
		Log info "Starting server on http://" + host + ":" + port
		listener = spawn HttpListener {
			port: port
			host: host
			delegate: me
		}
	}
}

server = spawn Server {
	host: "0.0.0.0"
}
